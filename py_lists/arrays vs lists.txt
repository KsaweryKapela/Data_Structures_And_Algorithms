Both:
    are mutable
    can be iterated and indexed
    can be sliced

Arrays:
    optimized for arithmetic computations, ex: can be divided
    my_array = numpy.array([1, 2, 3, 4])
    my_array/2 -> [0.5, 1, 1.5, 2]

    all data types must be the same